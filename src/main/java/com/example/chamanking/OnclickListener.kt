package com.example.chamanking

interface OnclickListener {
    fun onClick(item:itemProducto)
    fun onClickEvent(item:itemProducto)
    fun onClickPopular(item:itemProducto)
    fun onClickprodVent(item:itemprodVentas)
    fun eventopress(item:itemprodVentas)
}